{
  "rules": {
    ".read": false,
    ".write": false,
    "characters": {
      "$charId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['_updatedAt']) || !newData.exists()"
      }
    }
  }
}
